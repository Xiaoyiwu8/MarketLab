# Market Lab — Agent 工作说明

## 范围与入口

本文件适用于整个仓库。目标是维护美股与期权研究、回测和模拟交易平台；界面以中文为主，保留 PAPER ONLY 标识。除非用户明确提出实盘项目，不接入真实订单。

开始任务时：
1. 确认当前目录是本仓库，读取 `git status --short` 和最近提交，保留用户已有修改。
2. 按任务读取 `README.md` 对应功能与边界说明，并检查相关源码。README 中旧验证记录只是历史记录，不代表当前测试结果。
3. 从 `package.json` 确认可用命令和运行时要求；把本次修改限定到用户请求涉及的模块。

当前独立副本位于 `C:\Users\wuxiaoyi\Documents\Codex\Projects\MarketLab`。移动或克隆后以实际 Git 根目录为准。后续代码修改在此仓库进行，避免继续修改日期目录中的旧副本。

## 启动和验证

运行时要求以 `package.json` 的 engines 为准；依赖用 npm 和 `package-lock.json` 管理。

```sh
npm ci
npm run dev
```

打开开发服务器实际输出的地址。`npm run build` 生成生产产物后，`npm start` 使用 Wrangler 本地运行；启动本地服务不等于部署线上。

按修改范围验证：
- 计算、数据适配、账户或风控修改：运行 `npm test`，并覆盖受影响的边界行为。
- TypeScript、组件或构建配置修改：运行 `npm run typecheck` 和 `npm run build`；交互变更还需检查相关页面流程。
- 纯文档修改：检查路径、命令、差异和格式即可，无须重复完整构建。
- 需要格式或静态检查时查看 `package.json` 的 lint / format 命令；避免无关文件的批量格式化。

测试受凭证、网络或环境限制时，说明未验证的具体部分。模拟响应测试、公共接口实测和浏览器验证分别报告；不将一种验证当成另一种。

## 模块定位

| 任务 | 优先入口 |
| --- | --- |
| 首页与操作台 | `app/page.tsx`、`components/lab.tsx` |
| 数据与批量回测界面 | `components/data-center.tsx` |
| 行情与外部接口 | `app/api/`、`lib/providers.ts`、`lib/tencent.ts`、`lib/research.ts` |
| 指标、回测、期权与模拟账务 | `lib/engine.ts` |
| CSV 导入与命令行复现 | `lib/csv.ts`、`scripts/backtest.mjs` |
| 交易观察、趋势与成交量 | `lib/trade-guidance.ts`、`lib/short-guidance.ts`、`lib/review.ts`、`lib/volume.ts` |
| 震荡中枢与宏观时间 | `lib/range-center.ts`、`lib/event-time.ts`、`lib/rate-probability.ts` |
| 展示组件与样式 | `components/`、`components/ui/`、`app/globals.css` |
| 测试与历史样本 | `tests/`、`data/examples/` |
| 构建与托管配置 | `vite.config.ts`、`.openai/hosting.json`、`next.config.ts` |

计算逻辑优先放在 lib 的可测试函数中；外部请求通过现有 API / 适配层组织。沿用 React、TypeScript、vinext / Vite、Cloudflare 和 Sites 的现有结构；更改托管配置前检查其依赖关系。

## 必须保留的产品语义

- 行情展示来源、行情时间、延迟属性和复权口径。请求失败显示明确错误；演示数据只能显式选择，历史样本不能冒充当前行情。
- 指标遵循预热、样本不足、无效价格、零分母和未完成日线规则。时间处理区分 UTC、美东交易日期和北京时间，考虑夏令时。
- 回测避免未来数据泄漏，保留信号与成交时点、费用、滑点、跳空止损和回撤规则。输出保留输入数据与参数，方便复现；批量独立账户回测不能称作组合回测。
- 支撑压力与震荡区间的构建排除最新观察日，突破后使旧区间失效。演示、过期或不足数据不能生成可用入场计划，提示遵循现有风控优先级。
- 区分观察信号、回测结果和实际模拟成交。未验证的组合规则不能描述成已回测策略，评分不能描述成胜率。
- 股票模拟账户当前仅支持多头；做空观察不等于可开空。期权理论估值与实际报价分开，保留模型、合约乘数和指派处理的限制。
- 宏观事件和利率概率当前依赖手动录入。区分事件时间、来源更新时间、采样时间和本地录入时间；缺失的实时概率保持缺失。

## 数据、凭证与持久化

模拟账户、宏观事件和利率概率保存在站点 localStorage。迁移仓库、换域名或本地启动不会自动迁移这些记录；账户可从原页面导出 JSON。修改存储结构时，兼容或迁移旧记录，不能默默清空账户。

API 凭证不进入源码、Git、日志或示例文件；沿用现有凭证生命周期，新增持久化需明确设计。`.env*` 保持忽略。测试邮件或其他对外消息仅在用户明确授权后发送。

`data/examples/` 是历史验证样本。临时分析写入忽略的 `work/`，生成结果写入忽略的 `outputs/`，目录不存在时先创建。保留样本来源与时间，不提交依赖、构建缓存、个人账户导出或密钥。

## Git 与交付

- Git 保存代码与项目说明；浏览器数据和会话历史各自独立。新会话应依据仓库文档和实际状态恢复工作。
- 提交前查看 diff，仅暂存本次相关文件；用户要求保存或提交时创建描述实际变化的提交。保留既有历史，不覆盖用户未提交修改。
- GitHub / Gitee 备份以 `git remote -v` 的实际配置为准。用户指定并授权目标私有仓库后再配置与推送；本地 commit 不代表远端备份完成。
- 线上发布按用户已授权范围进行，发布前完成相关验证，发布后核对部署状态；不把本地构建成功称为线上更新。
- 功能、启动方式或限制发生变化时更新 README；协作流程变化时更新本文件。
- 完成时说明改动、实际验证、剩余限制，以及本次是否提交、推送或部署。避免将尚未执行的步骤写成已完成。
